---
description: 根据技术方案（techdoc.md）自动拆分开发任务，生成含状态、优先级、依赖关系、模块路径、验收标准和测试要求的结构化 tasks.md
argument-hint: [techdoc.md 路径或需求名称]
---

# 任务拆分

执行 `/task-splitter` skill，严格按照 skill 中的完整流程执行任务拆分。

将以下信息作为上下文传入 skill：

- 技术方案路径或需求名称：`$ARGUMENTS`（若为空，skill 将自动查找当前目录下最近修改的 `techdoc.md`）
