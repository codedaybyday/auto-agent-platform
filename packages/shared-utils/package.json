{
  "name": "@auto-agent/shared-utils",
  "version": "0.0.0",
  "private": true,
  "description": "Shared utilities for Auto Agent Platform",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist node_modules"
  },
  "dependencies": {
    "@auto-agent/shared-types": "workspace:*"
  },
  "devDependencies": {
    "tsup": "^8.5.1",
    "typescript": "^5.5.2"
  }
}
